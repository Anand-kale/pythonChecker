version: 2.2

jobs:
  BotMonitoring_1:
    docker:
      - image: circleci/python:3.9
    steps:
      - checkout
      - run:
          name: Print environment variable
          command: |
            echo "Environment Variable 'anand' for Job 1: $anand"
      - run:
          name: Run Python script for Job 1
          command: |
            python3 main.py $anand

  BotMonitoring_2:
    docker:
      - image: circleci/python:3.9
    steps:
      - checkout
      - run:
          name: Print environment variable
          command: |
            echo "Environment Variable 'anand' for Job 2: $anand"
      - run:
          name: Run Python script for Job 2
          command: |
            python3 main.py $anand

  BotMonitoring_3:
    docker:
      - image: circleci/python:3.9
    steps:
      - checkout
      - run:
          name: Print environment variable
          command: |
            echo "Environment Variable 'anand' for Job 3: $anand"
      - run:
          name: Run Python script for Job 3
          command: |
            python3 main.py $anand

workflows:
  version: 2
  commit-workflow:
    jobs:
      - BotMonitoring_1
      - BotMonitoring_2
      - BotMonitoring_3
